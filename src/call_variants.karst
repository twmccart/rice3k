#!/bin/bash

#PBS -l nodes=1:ppn=8,walltime=14:00:00,vmem=20gb
#PBS -k o
#PBS -j oe
#PBS -M twmccart@iu.edu
#PBS -m abe

ACCESSION=$1

# load modules for karst

ROOT="/N/dc2/scratch/twmccart/3KRiceReads/20cultivarphylogeny/VCFs"

cd $ROOT

source $ROOT/src/twmccart.environment

cd $CALLS

module load java tabix vcftools/0.1.14

call_variants $ACCESSION
