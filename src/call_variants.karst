#!/bin/bash

#PBS -l nodes=1:ppn=8,walltime=24:00:00,vmem=20gb
#PBS -k o
#PBS -j oe
#PBS -M twmccart@iu.edu
#PBS -m abe

accession=$1

# load modules for karst
module load java

root="/N/dc2/scratch/twmccart/VCFs"

cd $root

source $root/src/environment.sh

cd $calls

call_variants $accession
