#!/bin/bash

#PBS -l nodes=1:ppn=12,walltime=24:00:00,vmem=100gb
#PBS -k o
#PBS -j oe
#PBS -M twmccart@iu.edu
#PBS -m abe

accession=$1

# load modules for karst

root="/N/dc2/scratch/twmccart/VCFs"

cd $root

source $root/src/environment

cd $calls

module load java/1.8.0_40 tabix bcftools

full_genotype $accession
echo
echo "got full vcf, cleaning now"
echo
clean_and_split_vcf $accession
